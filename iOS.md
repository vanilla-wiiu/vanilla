# iOS Port of Vanilla Gamepad
The iOS port is using the code of the Android version, and uses the SDL3 framework to help make the port work. It's configured as a "Game" to make it able to use C as the language.

## To do
- Get the app to launch
- Fix the Build Settings
- Get an Apple Developer account
- Make the app compliant with Apple's design standards
- Add an App Logo
- Create a launch screen
- Add an iMessage sticker pack